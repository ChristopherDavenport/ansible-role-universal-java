---
- hosts: localhost
  pre_tasks:
  - name: apt update
    apt: update_cache=yes
    register: apt_result
    until: apt_result|success and not apt_result.changed
    retries: 3
    delay: 1
    when: ansible_os_family == 'Debian'
  roles:
    - role_under_test
