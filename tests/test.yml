---
- hosts: localhost
  pre_tasks:
  - name: apt update
    apt: update_cache=yes
    register: __update_cache
    until: not __update_cache.changed
    retries: 5
    ignore_errors: True
    when: ansible_os_family == 'Debian'
  roles:
    - role_under_test
